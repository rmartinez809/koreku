const { Fragment } = require("react")

const Header = (() => {
    return (
        <Fragment>
            <div className="header-container">
                <div>Home</div>
                <div>Sign Out</div>
            </div>
        </Fragment>
    )
})

export default Header